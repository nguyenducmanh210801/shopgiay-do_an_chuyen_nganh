from django.shortcuts import render
from django.http import HttpResponse

#def index(request):
 #   shopname = "Shoes H&M "
  #  sanpham = ["Giay the thao", "Giay chay bo", "Giay brand "]
   # context = {"name": shopname, "sanpham": sanpham}
    #return  render(request, "user/index.html", context)